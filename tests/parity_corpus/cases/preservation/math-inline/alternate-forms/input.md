Paren \(a + b\), GitLab $`c + d`$, GitLab two $``e + `f` + g``$, MyST {math}`h + i`, and MyST two {math}``j + `k` + l`` remain exact.

Inline environment \begin{smallmatrix}a & \begin{inner}b\end{inner}\\c & d\end{smallmatrix} remains exact.
