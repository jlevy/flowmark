Term "one"
Alias `with  spaces`

:   First "definition"... keeps $x_y$ and ``code  span``.
    lazy continuation stays exactly here.
~ Second definition with \(...\).

    Indented paragraph with "quotes"... and $a_b$.

Next term

:
    Block-only "definition"... with $z$.

Outside "changes"...
