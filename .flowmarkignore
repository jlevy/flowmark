# Files excluded from `flowmark --auto .` (e.g. `make format-docs`).
#
# AGENTS.md (and its CLAUDE.md symlink) contain a tbd-managed integration block
# with a mid-document `---` frontmatter fence. Reformatting reinterprets that
# fence as a setext heading and corrupts the block, so leave these to `tbd setup`.
AGENTS.md
CLAUDE.md

# Golden test fixtures: inputs are intentionally messy and expected outputs are
# fixed snapshots. Auto-formatting them would corrupt the test suite.
tests/

# Vendored tbd guideline/research copies and the tbd-managed skill mirror: these
# mirror upstream (with intentional emoji and formatting) and are owned by `tbd`.
docs/general/
.claude/

# The markdown-preservation spec documents the very constructs flowmark currently
# corrupts, so formatting it rewrites its own evidence: on 2026-08-25 a format pass
# re-paired the backticks in three code spans containing fenced-block info strings and
# the GitLab dollar-backtick form (issue #58). Excluded until #58 and the preservation
# work land, then re-check and remove this entry.
docs/project/specs/active/plan-2026-08-25-markdown-preservation.md
